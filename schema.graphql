# import Author from "schemas/author.graphql"
# import Post from "schemas/post.graphql"

type Query {
    posts: [Post]
    authors: [Author]
    test: String
}

type Mutation {
    post(id: Int!): Post
}

schema {
    query: Query
    mutation: Mutation
}