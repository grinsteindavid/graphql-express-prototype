# import Author from "schemas/author.graphql"
# import Post from "schemas/post.graphql"

type Query {
    posts: [Post]
    authors: [Author]
}

type Mutation {
    post(id: String): Post
}

schema {
    query: Query
    mutation: Mutation
}